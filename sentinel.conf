# specify path to aither.conf or leave blank
# default is the same as aitherCore
#aither_conf=/home/YOUR HOME DIRECTORY/.aithercore/aither.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
